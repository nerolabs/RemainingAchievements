# Wago Addons listing copy

Wago Addons uses **Markdown** for descriptions — identical to CurseForge — so
the **Description** is the same as the one in `curseforge-listing.md` (paste
that block verbatim). Only the short summary differs slightly in length limits;
use the one below. Logo: `assets/logo-512.png`; screenshots: `assets/screenshots/`
(order 1..8).

## Summary (short field)

Adds a "Remaining" tab to the Achievement UI: every incomplete achievement in one searchable, filterable list — stash, hidden achievements, Feats of Strength, opposite-faction view, and spreadsheet export. For completionists & achievement hunters.

## Description

Paste the **Description** section of `curseforge-listing.md` (from "**Ever
wondered what's actually left?**" through the closing "…puts it all on one
screen."). It is already CommonMark and renders correctly on Wago.
